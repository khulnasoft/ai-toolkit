import type { Metadata, Viewport } from 'next';
import { Inter, JetBrains_Mono } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-sans',
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ['latin'],
  variable: '--font-mono',
});

export const metadata: Metadata = {
  title: 'AI Toolkit | The AI Gateway For Developers',
  description:
    'The universal AI layer for building frameworks and agents. Switch between AI providers by changing a single line of code.',
  keywords: ['AI', 'SDK', 'TypeScript', 'OpenAI', 'Claude', 'Gemini', 'LLM', 'Agents'],
  openGraph: {
    title: 'AI Toolkit | The AI Gateway For Developers',
    description: 'The universal AI layer for building frameworks and agents.',
    type: 'website',
  },
};

export const viewport: Viewport = {
  themeColor: [
    { media: '(prefers-color-scheme: light)', color: 'white' },
    { media: '(prefers-color-scheme: dark)', color: '#0a0a0a' },
  ],
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
