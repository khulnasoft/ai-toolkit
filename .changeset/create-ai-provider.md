---
'create-ai-provider': major
---

Publish the `create-ai-provider` scaffolder CLI (`tools/create-ai-provider`) for generating repo-convention-complete provider packages (`openai-compatible`, `harness-acp`, `full-custom` archetypes).
