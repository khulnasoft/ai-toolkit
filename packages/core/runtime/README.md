# @ai-toolkit/runtime

Runtime-neutral capability contracts for browser, Node, Edge, Workers, serverless, mobile, Electron, and Tauri integrations.

The package intentionally contains no Node-only imports. Runtime-specific adapters should implement `RuntimeAdapter` in optional packages.
